// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    apply from: this.file("version.gradle")
    repositories {
        maven { url 'https://maven.aliyun.com/repository/public' }
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url "https://jitpack.io" }
    }
    dependencies {
        classpath "com.android.tools.build:gradle:${versions.androidGradleTools}"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${versions.kotlin}"
        classpath "com.billy.android:cc-register:${versions.cc_register}"

        //plugins
        classpath "com.wlqq.phantom:phantom-host-gradle:${versions.phantom.gradle}"
        classpath "com.wlqq.phantom:phantom-plugin-gradle:${versions.phantom.gradle}"
//        classpath "com.wlqq.phantom:phantom-gradle-plugin:${versions.phantom.gradle}"

    }
}

allprojects {
    repositories {
        maven { url 'https://maven.aliyun.com/repository/public' }
        maven { url 'https://maven.aliyun.com/repository/google' }
        maven { url "https://jitpack.io" }
        maven { url 'https://dl.bintray.com/umsdk/release' }
    }
}

/**
 *  maven { url 'https://mirrors.huaweicloud.com/repository/maven/' }*   jcenter()
 *   google()
 */

task clean(type: Delete) {
    delete rootProject.buildDir
}

